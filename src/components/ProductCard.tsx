import {Heart,ArrowUpRight} from 'lucide-react'; import {Link} from 'react-router-dom'; import type {Product} from '../types/catalog'; import {useFavorites} from '../hooks/useFavorites';
export function ProductCard({product}:{product:Product}){const {ids,toggle}=useFavorites();const fav=ids.includes(product.id);return <article className="product-card"><Link to={`/catalog/${product.slug}`}><img src={product.images[0]} alt={product.name} loading="lazy"/><span>{product.category}</span><h3>{product.name}</h3><b>${product.price.toLocaleString('en-US')}</b><ArrowUpRight className="corner"/></Link><button className={fav?'fav active':'fav'} onClick={()=>toggle(product.id)} aria-label="Guardar en favoritos"><Heart fill={fav?'currentColor':'none'}/></button></article>}
